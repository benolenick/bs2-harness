OS guess: Ubuntu Linux (service banners; exact release not established).
21/tcp FTP — vsftpd 3.0.3; anonymous login allowed; flag.txt listed read-only.
22/tcp SSH — OpenSSH 8.2p1 Ubuntu 4ubuntu0.5; 25/tcp SMTP — Postfix smtpd.
53/tcp DNS — unrecognized service banner/version string 1337_HTB_DNS.
80/tcp HTTP — Apache 2.4.41 (Ubuntu), title “Inlanefreight”; no CMS/framework identified.
110/tcp POP3 and 995/tcp POP3S — Dovecot pop3d; 143/tcp IMAP and 993/tcp IMAPS — Dovecot imapd.
111/tcp rpcbind v2–4; 8080/tcp HTTP — Apache 2.4.41 (Ubuntu), title “Support Center”; most likely attack surface: anonymous FTP.
PRIMARY CATEGORY: linux-service.
